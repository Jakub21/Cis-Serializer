from setuptools import setup, find_packages

setup(
  name = 'Cis',
  description = 'Protocol for encoding dictionaries into byte strings',
  version = '1.0.0',
  license = 'MIT',
  author = 'Jakub21',
  url = 'https://github.com/Jakub21/',
  packages = find_packages(),
)
